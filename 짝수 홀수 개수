import Foundation

func solution(_ num_list:[Int]) -> [Int] {
    var odd = 0
    var even = 0
    
    for i in 0..<num_list.count {
        if num_list[i] % 2 == 0 {
            even += 1
        } else {
            odd += 1
        }
    }
    return [even, odd]
}
