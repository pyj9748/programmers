import Foundation

func solution(_ my_string:String) -> String {
    var ans = ""
    let a = "aeiou"
    for c in my_string {
        if !a.contains(String(c)) {
            ans += String(c)
        }
    }
    return ans
}
