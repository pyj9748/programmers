import Foundation

func solution(_ n:Int) -> Int {
    var cnt = 1
    var num = 1
    while cnt != n + 1 {
        if num % 3 == 0 || String(num).contains("3") {
            num += 1
        } else {
            num += 1   
            cnt += 1
        }
        
    }
    return num - 1
}
