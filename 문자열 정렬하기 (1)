import Foundation

func solution(_ my_string:String) -> [Int] {
    var ans: [Int] = []
    for c in my_string {
        if let num = Int(String(c)) {
            ans.append(num)
        }
    }
    return ans.sorted()
}
