import Foundation

func solution(_ array:[Int]) -> Int {
    var ans = 0
    for num in array {
        let str = String(num)
        for c in str {
            if c == "7" {
                ans += 1
            }
        }
    }
    
    return ans
}
