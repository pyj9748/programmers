import Foundation

func solution(_ n:Int, _ numlist:[Int]) -> [Int] {
    var ans: [Int] = []
    numlist.forEach {
        if $0 % n == 0 {
            ans.append($0)
        }
    }
    
    return ans
}
